# WX0 zero-shot experiments only. Keep this environment separate from artifact
# builders because GLiNER requires a newer Transformers/Tokenizers pair.
gliner==0.2.24
sentence-transformers==5.1.2
psutil==7.1.3
numpy==1.26.4
onnx==1.17.0
onnxruntime==1.20.1
tokenizers==0.22.1
torch==2.5.1
transformers==4.57.3
