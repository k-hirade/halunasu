# 縦断患者コンテキスト mock再計測

## 結論

STGの同一revision `fee-api-stg-00166-7zk` で5ケースを計測した。全文抽出対照は各ケース3回、
合計15回実行し、確定明細・候補集合の対照内揺れは0件だった。

- 完全copy-forward: 別セッション参照と同一セッション再計算の双方で全文対照と一致した。
  `memoHitLineRatio=1`、新規/消失行0、OpenAI呼出し0だった。
- P行だけ変更: 継続3・新規1・消失1で、全文対照と一致した。
- 行為消失: 前回だけの創傷処置は現在の明細・候補に混入せず、`removedLineCount=1` と
  `extraction_memo` traceの双方に記録された。
- 全行変更: メモを使わず全文抽出へ戻り、全文対照と一致した。
- 院外処方箋の新規記載: 既知の`visit_facts`引継ぎ制約が実害を生じた。全文対照は
  処方箋料系だった一方、メモ経路には院内調剤・処方料とアムロジピン32点が残り、
  確定明細完全一致に不合格となった。

この結果を受け、新規または消失行に院内/院外処方・処方箋・一般名処方・リフィルの語彙が
ある場合は行スコープ抽出を使わず、全文抽出へフォールバックする修正を実装した。
特定の患者、SOAP本文、薬剤名には依存しない。抽出スナップショットschemaはv2へ更新し、
旧snapshotは自動失効する。

修正後のSTG再計測は2回実施した。

- [20260722_171002](./20260722_171002/README.md): 再デプロイ時に `FEE_EXTRACTION_MEMO` の
  環境変数が設定されず、メモ機構が無効のまま計測した**無効計測**（全ケース
  `extractionMemo.reason="disabled"`）。副産物として「メモ無効＝全文抽出」の等価性
  （確定明細差分なし）を確認する負の対照になった。再発防止はcloseout L4（pre-flight）。
- [20260722_172416](./20260722_172416/README.md)（**最新・有効**、revision fee-api-stg-00168-gbt）:
  4/5 pass、acceptance全合格。visit_facts処方フォールバックが実測で有効
  （`visit_facts_sensitive_change` 検知→全文抽出→対照一致）。唯一のfail（all_lines_new）は
  メモ不使用シナリオでの単発のLLM取りこぼし（評価経路1回だけ抽出0イベント）で、
  帰属ルール上 `inconclusive_llm_variability`。したがって、**メモ受入は5/5、意味等価性は
  4/5合格+1件判定不能**。性能: 完全一致 約11.3秒→約0.93秒
  （92%短縮）、行為消失 約9.8秒→約0.71秒（93%短縮）、P行変更 約11.5秒→約5.8秒（50%短縮、
  ただし入力5,029トークンは全て非キャッシュでコスト削減は未証明）。

残作業と PROD 有効化ゲートは
`docs/implplan/fee-longitudinal-phase1-closeout-20260722.md`（L1空抽出ガード・L3基準日
2026-06-11修正・L7元mock時系列再測定 ほか）に集約した。

## 計測結果

- 最新の修正前実測: [20260722_155039](./20260722_155039/README.md)
- 初回計測（OpenAI呼出し判定の誤検知を含む）: [20260722_153654](./20260722_153654/README.md)
- calculate応答時間: median 8,581.21ms / mean 7,848.48ms / max 11,556.11ms
- 履歴取得不能: 0件
- 対照内のLLM揺れ: 0件

## 指標の注意

制御データはlineKey一致率を人工的に設定している。ここで得られるLLM削減率は機構の上限性能で
あり、実運用のcopy-forward率や実効削減率ではない。顧客の実カルテで再計測するまで不明と扱う。
