# mock_partner STG再算定比較（3患者）

初回実行: 2026-07-13 11:05 JST / `mockpartner-20260713020527479`  
再測定: 2026-07-13 11:35 JST / `mockpartner-20260713023517795`  
対象環境: STG  

> 初回は `fee-api-stg-00150-6rp`、再測定はJSONL修正後の `fee-api-stg-00151-mm6`。入力・実行回数・逐次実行条件は同一。

## 結論

- 初回・再測定とも患者別ZIPを12回送信し、合計24/24リクエストがHTTP 200だった。
- UKEは3患者とも患者x月のレセとして正常に読み込まれ、既存レセ15コード、8受診、25オーダーを識別できた。
- JSONL修正後、カルテ取込は `0/8件` から `8/8件` へ改善した（1001: 2件、1006: 4件、1013: 2件）。
- 当社エンジンの出力は全患者0コードだったため、一致は0/15コード、再現失敗は15/15コードとなった。
- 再現失敗15コードのうち11コードはAPIが明示する「当社未対応（在宅）」、4コードは一般領域のエンジンギャップだった。
- 修正後のウォーム時Cloud Run時間は中央値375.75ms、最小247.46ms、最大407.29ms。初回リクエストは13.07秒だった。

JSONL取込不具合は解消した。ただし、再算定差分APIはカルテをpayloadへ格納しても通常セッションのOpenAI抽出を実行しないため、依然として「カルテからの算定精度」の評価には使えない。算定結果が変わらなかったことは、この責務差と在宅未対応を示している。

## 横断結果

| 患者 | 受診 | カルテ取込 | 既存レセ | 既存点数 | 当社コード | 一致 | 再現失敗 | 在宅未対応 | 一般ギャップ | API中央値 |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| [1001](./1001/README.md) | 2 | 0→2 | 3コード | 1,988点 | 0 | 0 | 3 | 3 | 0 | 265.03ms |
| [1006](./1006/README.md) | 4 | 0→4 | 6コード | 4,326点 | 0 | 0 | 6 | 5 | 1 | 390.17ms |
| [1013](./1013/README.md) | 2 | 0→2 | 6コード | 2,438点 | 0 | 0 | 6 | 3 | 3 | 335.27ms |
| **合計** | **8** | **0→8** | **15コード** | **8,752点** | **0** | **0** | **15** | **11** | **4** | **375.75ms（全9回）** |

`API中央値`はウォームアップを除くCloud Runのサーバ時間。既存レセは正解データではないため、0/15は「一致度」であり医学的・請求上の正解率ではない。

## パフォーマンス

### ウォーム後の修正前後比較（各9回）

| 指標 | クライアント修正前 | クライアント修正後 | Cloud Run修正前 | Cloud Run修正後 |
|---|---:|---:|---:|---:|
| 最小 | 279.19ms | 269.26ms | 263.48ms | 247.46ms |
| 中央値 | 384.82ms | 392.47ms | 363.21ms | 375.75ms |
| 平均 | 410.58ms | 364.04ms | 375.71ms | 343.74ms |
| p95（参考） | 669.42ms | 450.64ms | 600.51ms | 407.29ms |
| 最大 | 669.42ms | 450.64ms | 600.51ms | 407.29ms |

- 修正後のCloud Run中央値は約3.5%上振れ、平均は約8.5%短縮した。標本が小さいため、JSONL修正による有意な性能差とは判断しない。
- 標本数は9回しかないため、p95は容量計画やSLA判定には使えない。
- 逐次実行のみで、同時実行性能は測っていない。
- 対象ルートには内部ステージ別ログがなく、ZIP解析、UKE解析、Python算定、差分生成の個別時間は分離できなかった。

### 初回リクエスト

両測定の最初の患者1001は13秒以上を要した。修正前はCloud Runログに `Starting new instance` がありコールドスタートを確認できた。修正後は同時間帯に起動ログがなく、Python workerやマスター展開等を含む初回初期化の内訳は分離できない。

| 測定 | クライアント | Cloud Run | 判定 | リビジョン |
|---|---:|---:|---|---|
| 修正前 | 14,526.53ms | 14,250.56ms | Cloud Run起動を確認 | `fee-api-stg-00150-6rp` |
| 修正後 | 13,203.62ms | 13,069.18ms | 初回初期化（内訳不明） | `fee-api-stg-00151-mm6` |

- サービス設定: `minScale`指定なし、`maxScale=3`、CPU throttling有効

## 入力不足

3患者の `unknowns.csv` は合計43行だった。

| 種別 | 件数 | 影響 |
|---|---:|---|
| コメント・非請求 | 17 | 請求明細へ変換しないため妥当 |
| 薬剤コード・構造化数量不足 | 8 | 薬剤料、処方関連、用量点検を評価できない |
| 診療日単位の病名にできない | 11 | 適応病名、管理料、査定リスクを評価しにくい |
| マスター完全一致なし | 7 | 在医総管等の一部を比較対象へ入れられない |

JSONL修正後はカルテ8件をすべてAPI入力へ取り込めた。ただし、上表43件の元データ不足は変わらない。

## 改善優先順位

1. **完了: JSONL取込修正**  
   `jsonl` / `ndjson` を1行ずつ解析し、STGで8/8件の取込を確認した。
2. **P0: 評価経路の責務明確化**  
   現ルートは構造化ordersを決定論エンジンへ渡しており、通常セッションのOpenAI臨床情報抽出を通らない。「カルテから再算定」を評価する場合は、カルテ抽出を含む専用バッチ経路が必要。
3. **P1: 在宅算定範囲の実装**  
   11/15コードが設計上の在宅未対応。在宅患者訪問診療料、在医総管系加算、訪問診療時ベースアップ評価料を要件・施設基準・月回数とともに実装する。
4. **P1: 一般4コードの再現経路確認**  
   がん性疼痛緩和指導管理料、薬剤総合評価調整管理料、療養費同意書交付料、傷病手当金意見書交付料が構造化ordersにあるのに出力されない原因を分離する。
5. **P2: ステージ別計測**  
   再算定差分診断に、ZIP/UKE解析、payload生成、患者別Python算定、差分生成、監査保存の時間を構造化ログで追加する。

## 測定資料

- [測定方法](./methodology.md)
- [患者1001](./1001/README.md)
- [患者1006](./1006/README.md)
- [患者1013](./1013/README.md)
