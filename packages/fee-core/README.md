# Fee Core

Product-owned fee calculation session builders and local mock calculation logic.

This package does not call GCP, OpenAI, or sibling product services. It stores Platform IDs and snapshots passed in by `fee-api`.
