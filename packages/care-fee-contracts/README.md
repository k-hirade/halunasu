# Care Fee Contracts

Versioned CSV and API contracts for long-term care fee checks.

The package normalizes structured facts only. It does not infer clinical facts
from free text and does not contain billing units or eligibility rules.
