# Care Fee Core

Deterministic long-term care fee checks. The core accepts normalized facts and
an effective-dated care service code master. It never calls an LLM, reads Fee
sessions, or writes application state.
