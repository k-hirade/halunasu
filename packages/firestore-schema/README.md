# Firestore Schema

Shared Firestore collection names, path builders, and schema helpers.

This package should make it harder for product services to accidentally write outside their intended boundaries.

Implemented first:

- Platform collection names
- Organization/member/patient/product document path builders
- Login identity key/path helpers
